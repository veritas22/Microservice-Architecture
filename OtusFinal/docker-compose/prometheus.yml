global:
  scrape_interval: 5s

scrape_configs:
  - job_name: prometheus
    metrics_path: /metrics
    static_configs:
      - targets:
          - otel-collector:8889
        labels:
          group: otel-collector
    tls_config:
      insecure_skip_verify: true 